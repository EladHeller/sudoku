{
  "name": "sudoku",
  "version": "1.0.0",
  "description": "A browser-based Sudoku solver",
  "private": true,
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test .test-dist/__tests__/*.test.js",
    "check": "npm run typecheck && npm test && npm run build && npm audit --audit-level=low"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2",
    "vite": "8.2.0"
  },
  "dependencies": {
    "rxjs": "7.8.2"
  }
}
